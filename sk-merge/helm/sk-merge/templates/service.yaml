---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sk-merge.serviceName" . }}
  labels:
    {{- include "sk-merge.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "sk-merge.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.server.port }}
      targetPort: {{ .Values.server.port }}

