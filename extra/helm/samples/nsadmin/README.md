# nsadmin

A sample helm chart which create a admin user for an existing namespace.

This admin user is associated with a role granting all permissions on all objects of the namespace in which the chart is deployed.


