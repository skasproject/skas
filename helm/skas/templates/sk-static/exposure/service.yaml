{{ if and .Values.skStatic.enabled .Values.skStatic.exposure.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "skStatic.serviceName" . }}
  labels:
    {{- include "skas.labels" . | nindent 4 }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  selector:
    {{- include "skas.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.skStatic.serverPort }}
      targetPort: {{ .Values.skStatic.serverPort }}
{{ end }}
